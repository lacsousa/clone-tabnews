Some installations 

    - npm install --save-dev jest@29.6.2

    - We are using postgres in  Database 

    - Trough Docker Compose ( compose.yaml )

        ** Using detached
        - docker compose up -d

    - To use a Postgres client    ( pgsql )
        
        sudo apt update && sudo apt upgrade
        sudo apt install postgresql-client

        - Add ports in yaml 
        - Restart the container
            docker compose up -d --force-recreate

        - To execute -> psql
             psql --host=localhost --username=postgres --port=5432

    We move compose.yaml, so now we need from other docker compose command

        To start database -> docker compose -f infra/compose.yaml up -d 

            You can open this service in another tab and after close this tab, 
                because the service comes up detached

    - Install pg ( Connect to the postgresql by nodejs - node-postgres.com)
        npm install pg@8.16.3
